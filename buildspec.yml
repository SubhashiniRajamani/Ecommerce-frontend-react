version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 22

  pre_build:
    commands:
      - npm ci --legacy-peer-deps
      - npm run build

  build:
    commands:

artifacts:
  files:
    - build/**/*
    - appspec.yml
    - scripts/**
