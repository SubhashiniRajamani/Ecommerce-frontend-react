version: 0.0
os: linux
files:
  # Specify the source and destination files/directories if needed
  # For example:
  - source: /
    destination: /home/ubuntu/Ecommerce-backend-mern-main 

hooks:
  BeforeInstall:
    - location: scripts/before_install.sh
      timeout: 300
      runas: root
  AfterInstall:
    - location: scripts/after_install.sh
      timeout: 300
      runas: root
