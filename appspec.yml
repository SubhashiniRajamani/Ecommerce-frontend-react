version: 0.0
os: linux

files:
  - source: /
    destination: /var/www/ecommerce-frontend

hooks:
  BeforeInstall:
    - location: scripts/before_install.sh
      timeout: 300
      runas: root

  ApplicationStart:
    - location: scripts/after_install.sh
      timeout: 300
      runas: root
